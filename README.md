# cloud

Criome cloud-provider API runtime.

This repo is intentionally documentation-only at birth. The contracts are live
in `signal-cloud` and `owner-signal-cloud`; runtime binaries should land only
with a real daemon request path.
